const assert=require('node:assert/strict');
const {chromium}=require('C:/Users/Tirth Patel/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const browser=await chromium.launch({headless:true,channel:'msedge'});try{const page=await browser.newPage({viewport:{width:1440,height:1100}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:8080/?engine=api&v=3');await page.screenshot({path:'tests/premium-empty.png',fullPage:true});const response=page.waitForResponse(r=>r.url().endsWith('/api/detect/image'));await page.locator('#media-input').setInputFiles('platevision/data/indian-plates/vid-3/frame-2600.jpg');const data=await(await response).json();await page.waitForFunction(()=>document.querySelectorAll('#result-rows tr').length>=2);assert.ok(data.detections.length>=2);assert.equal(data.vehicles.length,0);assert.equal(await page.locator('.vehicle-card').count(),data.detections.length);console.log('plates',data.detections.map(d=>({text:d.normalized_text,vehicle:d.vehicle_id})));console.log('coverage',await page.locator('#coverage').innerText());await page.screenshot({path:'tests/premium-multi-desktop.png',fullPage:true});await page.setViewportSize({width:390,height:844});await page.screenshot({path:'tests/premium-multi-mobile.png',fullPage:true});assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);await page.locator('#reset-button').click();assert.equal(await page.locator('.vehicle-card').count(),0);assert.deepEqual(errors,[]);console.log('multi-plate mapping, all vehicle cards, mobile overflow and reset passed');}finally{await browser.close();}})().catch(e=>{console.error(e);process.exit(1)});
